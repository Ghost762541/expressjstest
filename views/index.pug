html
  head
    title="Аккаунты"
    link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
  body
    a(href="/new") Add
    //-button(type='button' class=' c-btn-blue c-btn-circle c-btn-uppercase' value="Read More" onclick='gotomain()') Add
    //- button#BubbleButton.button.hypeButton.button-3d.button-caution.button-circle.button-jumbo(onclick = 'onButtonClick()') Add
    //- script.
    //-   function gotomain (){
    //-     $.ajax({
    //-     "url": "/new",
    //-     "method": "POST"
    //-     }); 
    //-   }
    p= Account.title
    p= Account.accountId

    //-p= accounts
    each account in accounts 
      p= account.title
      p= account.accountId
      p= account._id
      a(href="/delete/" + account._id) Del